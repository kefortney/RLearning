# VISUALIZATIONS

# Most common charts and visualization package
install.packages("ggplot2")
library(ggplot2)
# An implementation of an interactive grammar of graphics, taking the best parts of 'ggplot2', combining them with the reactive framework of 'shiny' and drawing web graphics using 'vega'.
install.packages("ggvis")
library(ggvis)
# R interface to Google's chart tools, allowing users to create interactive charts based on data frames. Charts are displayed locally via the R HTTP help server. A modern browser with an Internet connection is required and for some charts a Flash player. The data remains local and is not uploaded to Google.
install.packages("googleVis")
library(googleVis)
# Easily make interactive, web apps with R. A perfect way to explore data and share findings with non-programmers.
install.packages("shiny")
library(shiny)

# MACHINE LEARNING

install.packages("randomForest")
library(randomForest)
# used to build classification or regression models using a two stage procedure and the resultant models is represented in the form of binary trees.
install.packages("rpart")
library(rpart)
# If missing values are something which haunts you then MICE package is the real friend of yours.
install.packages("mice")
library(mice)
#Functions and Datasets to Accompany J. Fox and S. Weisberg, An R Companion to Applied Regression
install.packages("car")
library(car)


# DATA MANIPULATION

# Essential shortcuts for subsetting, summarizing, rearranging, and joining together data sets. dplyr is our go to package for fast data manipulation.
install.packages("dplyr")
library("dplyr")
# Tools for changing the layout of your data sets. Use the gather and spread functions to convert your data into the tidy format, the layout R likes best
install.packages("tidyr")
library("tidyr")
# Easy to learn tools for regular expressions and character strings.
install.packages("stringr")
# ls that make working with dates and times easier.
install.packages("lubridate")
library(lubridate)
#for creating and working with a new form of csv, transferrable between python and R
install.packages("feather")
library(feather)
# The broom package takes the messy output of built-in functions in R, such as lm, nls, or t.test, and turns them into tidy data frames.
install.packages("broom")
library(broom)

# IMPORT DATA

install.packages("xlsx")
library(xlsx)
install.packages("jsonlite")
library(jsonlite)
install.packages("XML")
library(XML)

# DATASETS

install.packages("datasets")
library(datasets)

# Text data mining package
install.packages("tidytext")
library(tidytext)


install.packages("caret")
install.packages("zoo")
install.packages("class")
# dashboard for html and build to hold markdown
install.packages("flexdashboard")
install.packages("shiny")
install.packages("gmodels")
library(plyr)
# library("xlsx")
# A fast way to build interactive (javascript based) visualizations with R. Packages that implement htmlwidgets
install.packages("htmlwidgets")
install.packages("leaflet")
install.packages("tilegramsR")
library(tilegramsR)

library(leaflet)


# https://support.rstudio.com/hc/en-us/articles/201057987-Quick-list-of-useful-R-packages
# R Markdown - The perfect workflow for reproducible reporting. Write R code in your markdown reports. When you run render, R Markdown will replace the code with its results and then export your report as an HTML, pdf, or MS Word document, or a HTML or pdf slideshow. The result? Automated reporting. R Markdown is integrated straight into RStudio.